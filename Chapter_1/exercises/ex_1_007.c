/* Exercise 1-7. Write a program to print the value of EOF . */

#include <stdio.h>

main() {
    printf("Value of EOF = %d\n",EOF);
}

/* The symbolic constant EDF is defined in <•tdlo.h>. The EDF outside the
double quotes in pr 1 n t f C > is replaced by whatever text follows
#define
EDF
in the include file. In our system EDF is - I, but it may vary from system to
system. That's why standard symbolic constants like EDF help make your pro­
gram portable. */
